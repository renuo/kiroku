<section class="hero is-medium is-primary">
  <div class="hero-body">
    <p class="title"><%= t("hero_title", scope: "home.index") %></p>
    <p class="subtitle"><%= t("hero_subtitle", scope: "home.index") %></p>
    <%= button_to "Sign in with Google", "/auth/google_oauth2", method: :post,
        class: "button is-white is-large", data: { turbo: false } %>
  </div>
</section>
